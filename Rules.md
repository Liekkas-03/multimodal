<!-- 这个文件用于定义项目开发与协作过程中必须遵守的规则。 -->

## 强制规则

1. 大依赖安装确认规则：当需要在本地安装超过 `500MB` 的环境依赖包时，必须先向用户确认。
2. 注释规范合并规则：我新写的文件需要在文件开头加入中文注释说明文件作用；我新写的函数需要在函数定义附近加入一句中文注释说明函数作用。
3. 文件命名规则：文件命名需精简且表达主要含义，最多使用一个 `_`。
4. 当需要在本地环境中安装超过500MB的依赖包时，需要向我询问，我同意后才能安装。